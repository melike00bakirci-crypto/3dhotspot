# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# --- Test / tooling caches ---
.pytest_cache/
.pytest-full-suite.lock
.coverage
.coverage.*
htmlcov/
.mypy_cache/
.ruff_cache/

# --- Pipeline outputs (generated; not source) ---
# results/ holds multi-GB per-gene run trees incl. >100MB archives.
results/
# Alternate roots passed via --results-root (verification runs, replays,
# rename experiments). Same multi-GB trees, same reason.
results_*/
logs/

# Per-gene analysis archives produced from results/
*_analiz.tar.gz
*.tar.gz

# --- Local, machine-specific Claude Code state ---
.claude/settings.local.json
.claude/agents/*.bak

# --- OS cruft ---
.DS_Store
Thumbs.db
